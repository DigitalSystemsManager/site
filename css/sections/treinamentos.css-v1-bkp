/* ====================================================
   DIGITAL SYSTEMS MANAGING - CSS: Sessão 3 Treinamentos e Aulas
   (Nível Hard de Inovação e Vendas)
==================================================== */

.sessao-treinamentos {
    background: linear-gradient(135deg, var(--cinza-claro) 0%, var(--branco) 100%); /* Fundo claro e limpo */
    color: var(--cinza-escuro);
    padding: 7rem 0;
    position: relative;
    overflow: hidden;
    border-top: 1px solid rgba(var(--azul), 0.1);
}

/* Títulos da Sessão */
.header-sessao-treinamentos {
    text-align: center;
    margin-bottom: 5rem;
}

.titulo-sessao-treinamentos {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    color: var(--azul-escuro);
}
.titulo-sessao-treinamentos .cor-destaque-dourado {
    color: var(--dourado);
    -webkit-text-fill-color: var(--dourado);
    background: none;
}

.subtitulo-sessao-treinamentos {
    font-size: clamp(1.2rem, 2.5vw, 1.6rem);
    color: var(--cinza-medio);
    max-width: 900px;
    margin: 0 auto 3rem;
    font-weight: 400;
    line-height: 1.5;
}

/* Blocos de Treinamento Individuais */
.treinamento-bloco {
    background: var(--branco);
    border-radius: 20px;
    padding: 4rem;
    margin-bottom: 4rem;
    box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    transition: var(--transicao-media);
    text-align: center;
    border: 1px solid var(--azul);
}

.treinamento-bloco.tipo-trilha {
    border-color: var(--verde-forte);
    box-shadow: 0 10px 30px rgba(var(--verde-forte-rgb, 39, 174, 96), 0.1);
}
.treinamento-bloco.tipo-particulares {
    border-color: var(--dourado);
    box-shadow: 0 10px 30px rgba(var(--dourado-rgb, 246, 180, 1), 0.1);
}
.treinamento-bloco.tipo-minicursos {
    border-color: var(--azul);
    box-shadow: 0 10px 30px rgba(var(--azul-rgb, 4, 94, 191), 0.1);
}

/* Titulos dos Blocos */
.titulo-bloco {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    color: var(--azul-escuro);
    margin-bottom: 1.5rem;
    line-height: 1.2;
}
.titulo-bloco .cor-verde-forte {
    color: var(--verde-forte);
    -webkit-text-fill-color: var(--verde-forte);
    background: none;
}
.titulo-bloco .cor-destaque-dourado {
    color: var(--dourado);
    -webkit-text-fill-color: var(--dourado);
    background: none;
}

.descricao-bloco {
    font-size: 1.1rem;
    color: var(--cinza-medio);
    max-width: 800px;
    margin: 0 auto 2.5rem;
    line-height: 1.6;
}

.lista-beneficios-trilha {
    list-style: none;
    padding: 0;
    margin: 0 auto 2.5rem;
    max-width: 700px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1.5rem 2.5rem; /* Gap horizontal e vertical */
}
.lista-beneficios-trilha li {
    font-size: 1.1rem;
    color: var(--azul-escuro);
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.8rem;
    text-align: left;
}

.btn-treinamento {
    padding: 1rem 3rem;
    font-size: 1.1rem;
    font-weight: 700;
}
.btn-treinamento.btn-primary { /* Sobrescrita do gradiente base para o dourado puro */
    background: var(--gradiente-cta);
    color: var(--azul-escuro); /* Texto da marca no dourado */
    box-shadow: var(--sombra-dourada);
}
.btn-treinamento.btn-primary:hover {
    transform: translateY(-5px) scale(1.02);
    box-shadow: 0 8px 30px rgba(var(--dourado-rgb, 246, 180, 1), 0.6);
}

.texto-suporte {
    font-size: 0.9rem;
    color: var(--cinza-medio);
    margin-top: 1rem;
}


/* ACORDEÃO PARA AULAS PARTICULARES */
.accordion-container {
    max-width: 900px;
    margin: 2rem auto 3rem;
    text-align: left;
    border-radius: 10px;
    overflow: hidden;
    border: 1px solid var(--azul);
    box-shadow: 0 5px 20px rgba(var(--azul), 0.1);
}

.accordion-item {
    border-bottom: 1px solid rgba(var(--azul), 0.2);
}
.accordion-item:last-child {
    border-bottom: none;
}

.accordion-header {
    background: linear-gradient(90deg, var(--azul), #0b519f); /* Fundo azul do item */
    color: var(--branco);
    padding: 1.5rem 2rem;
    width: 100%;
    text-align: left;
    border: none;
    outline: none;
    font-size: 1.2rem;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transicao-rapida);
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.accordion-header:hover {
    background: linear-gradient(90deg, #0b519f, var(--azul-escuro)); /* Um pouco mais escuro no hover */
}
.accordion-header .icon-toggle {
    font-size: 1.5rem;
    font-weight: 900;
    transition: transform 0.3s ease;
}
.accordion-header.active .icon-toggle {
    transform: rotate(45deg); /* Gira o + para um X quando aberto */
}

.accordion-content {
    padding: 0 2rem;
    background: var(--branco);
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s ease-out; /* Suavidade na abertura/fechamento */
}
.accordion-content.open {
    max-height: 500px; /* Valor arbitrário grande para garantir abertura completa */
    padding: 1.5rem 2rem;
}

.lista-assuntos {
    display: flex;
    gap: 2rem; /* Espaço entre as colunas */
    flex-wrap: wrap; /* Para envolver no mobile */
}
.lista-assuntos ul {
    list-style: none;
    padding: 0;
    margin: 0;
    flex: 1; /* Ocupa espaço igual */
    min-width: 250px; /* Garante que não fiquem muito estreitos */
}
.lista-assuntos li {
    font-size: 1rem;
    color: var(--cinza-escuro);
    margin-bottom: 0.8rem;
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
}
.lista-assuntos li::before {
    content: '→'; /* Seta como bullet */
    color: var(--dourado); /* Dourado para o bullet */
    font-weight: bold;
    flex-shrink: 0;
}


/* MINI-CURSOS GRAVADOS (Mini Galeria) */
.minicursos-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2.5rem;
    margin-top: 2.5rem;
    justify-items: center;
}

.card-minicurso {
    background: var(--azul-escuro); /* Fundo do card em azul escuro */
    border-radius: 15px;
    padding: 1.8rem;
    text-align: center;
    transition: var(--transicao-rapida);
    border: 1px solid rgba(var(--azul), 0.2);
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 100%; /* Garante altura igual se necessário com JS */
}
.card-minicurso:hover {
    transform: translateY(-8px) scale(1.01);
    box-shadow: 0 10px 25px rgba(var(--azul-escuro-rgb, 14, 61, 113), 0.3);
    border-color: var(--dourado);
}

.minicurso-thumb {
    width: 100%;
    max-height: 150px; /* Altura máxima da imagem */
    object-fit: cover; /* Recorta a imagem para caber */
    border-radius: 10px;
    margin-bottom: 1.5rem;
    border: 1px solid var(--dourado);
    box-shadow: var(--sombra-suave);
}

.titulo-minicurso {
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--branco);
    margin-bottom: 0.8rem;
    line-height: 1.3;
}

.descricao-minicurso {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 1.5rem;
    flex-grow: 1; /* Para alinhar botões */
}

.btn-minicurso {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.8rem 1.5rem;
    font-size: 0.9rem;
    font-weight: 600;
    border-radius: 50px;
    background: var(--dourado); /* Botão em dourado para destaque */
    color: var(--azul-escuro);
    transition: var(--transicao-rapida);
    margin-top: auto; /* Para alinhar na base */
}
.btn-minicurso:hover {
    background: #ffcc00;
    transform: translateY(-2px);
    box-shadow: 0 3px 10px rgba(var(--dourado-rgb, 246, 180, 1), 0.4);
}
.btn-minicurso .icon-play {
    margin-right: 0.5rem;
    font-size: 0.8rem;
}


/* CTA Final da Sessão */
.cta-final-treinamentos {
    text-align: center;
    background: linear-gradient(135deg, var(--azul) 0%, #001f3f 100%);
    padding: 4rem;
    border-radius: 15px;
    margin-top: 6rem;
    box-shadow: 0 0 30px rgba(var(--azul), 0.2);
    border: 1px solid var(--dourado);
}
.titulo-cta-final {
    font-size: clamp(2.2rem, 3.5vw, 3rem);
    color: var(--branco);
    font-weight: 900;
    margin-bottom: 2rem;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
}

.btn-acao-final {
    padding: 1.2rem 3rem;
    font-size: clamp(1.2rem, 2.5vw, 1.6rem);
    font-weight: 900;
    min-width: 350px;
    box-shadow: 0 0 40px rgba(var(--dourado-rgb, 246, 180, 1), 0.6); /* Grande brilho para CTA */
}
.btn-acao-final:hover {
    transform: translateY(-5px) scale(1.02);
    box-shadow: 0 0 60px rgba(var(--dourado-rgb, 246, 180, 1), 0.8);
}


/* Variáveis RGB para RGBA */
:root {
    --verde-forte-rgb: 39, 174, 96;
    --dourado-rgb: 246, 180, 1;
    --azul-rgb: 4, 94, 191;
    --azul-escuro-rgb: 14, 61, 113;
}


/* Responsividade */
@media (max-width: 992px) {
    .treinamento-bloco {
        padding: 3rem;
    }
    .lista-beneficios-trilha li {
        min-width: 250px;
    }
    .minicursos-grid {
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
    }
    .card-minicurso {
        padding: 1.5rem;
    }
}

@media (max-width: 768px) {
    .sessao-treinamentos {
        padding: 5rem 0;
    }
    .header-sessao-treinamentos {
        margin-bottom: 3rem;
    }
    .titulo-sessao-treinamentos {
        font-size: clamp(2rem, 8vw, 3.5rem);
    }
    .subtitulo-sessao-treinamentos {
        font-size: clamp(1rem, 3.5vw, 1.4rem);
    }
    .treinamento-bloco {
        padding: 2.5rem;
        margin-bottom: 3rem;
    }
    .titulo-bloco {
        font-size: clamp(1.6rem, 5vw, 2.5rem);
    }
    .descricao-bloco {
        font-size: 1rem;
    }
    .lista-beneficios-trilha {
        gap: 1rem 1.5rem;
        text-align: center;
        flex-direction: column; /* Coloca itens em coluna no mobile */
    }
    .lista-beneficios-trilha li {
        font-size: 1rem;
        justify-content: center; /* Centraliza o texto */
    }

    .accordion-container {
        margin: 1.5rem auto 2.5rem;
    }
    .accordion-header {
        font-size: 1rem;
        padding: 1rem 1.5rem;
    }
    .accordion-content.open {
        padding: 1rem 1.5rem;
    }
    .lista-assuntos {
        flex-direction: column;
        gap: 1rem;
    }
    .lista-assuntos ul {
        min-width: unset;
    }
    .minicursos-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }
    .card-minicurso {
        padding: 1.2rem;
    }
    .titulo-minicurso {
        font-size: 1.15rem;
    }
    .btn-minicurso {
        font-size: 0.8rem;
        padding: 0.7rem 1.2rem;
    }
    .cta-final-treinamentos {
        padding: 3rem;
        margin-top: 4rem;
    }
    .titulo-cta-final {
        font-size: clamp(1.8rem, 6vw, 2.5rem);
    }
    .btn-acao-final {
        min-width: unset;
        width: 100%;
        font-size: 1.1rem;
        padding: 1rem 2rem;
    }
    .texto-suporte {
        font-size: 0.8rem;
    }
}
